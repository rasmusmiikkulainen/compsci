1. self.x is an instance variable, x passed into init() is a parameter. x in origin() is a local variable. x outside everything is a global variable.
2. class variable
3. after class it specifies the name of the created class. when the function Point() is called, it creates an object of that class using the dunder init function.

4.1  
When the function origin() is called, since origin() calls the function Point(), which calls the init function.  
4.2  
When print(x) is called, since the dunder str function is called when the object needs to be represented in string form, like when printing.