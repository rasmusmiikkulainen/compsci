1. An object is an instance of a class. For example "abc" is an object and str is a class.

2. Train, Passenger have aggregation. Train has passengers. Passenger, Train have dependency. Passengers use train.  
Student, course have aggregation. Course has students. Student, course have dependency. Students take a class.  
Table, Furniture have inheritance. Table is furniture.  
Parent, Child have dependency. Child uses parent. Parent, child have aggregation. Parent has a child.  
Dependency. Book is literature. Aggregation. Book has literature.